version: '3'
services:
  react_app:
    build:
      dockerfile: ./Dockerfile
      context: .
    image: edsoares/node
    container_name: react-application
    ports:
      - "3000:3000"
    networks:
      - production-network

networks:
  production-network:
    driver: bridge